import React from 'react';

const SelectedProduct = ({ product }) => {
    const { img, leftName } = product;
    return (
        <div>
            <h2>Selected Product</h2>
        </div>
    );
};

export default SelectedProduct;